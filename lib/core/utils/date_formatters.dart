import 'package:intl/intl.dart';

class DateFormatters {
  static final DateFormat shortDate = DateFormat('MMM d, yyyy');
  static final DateFormat shortDateTime = DateFormat('MMM d, yyyy • h:mm a');
}
